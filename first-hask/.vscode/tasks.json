{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Haskell Program",
            "type": "shell",
            "command": "stack build && stack run",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}